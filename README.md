# news-scraper-flask
Built a Flask web app that searches Google News RSS by keyword, selected country, and last-24-hours filter. The app sends HTTP requests, parses XML with BeautifulSoup, extracts article titles, links, sources, and publication dates, then stores search history and results in SQLite using SQLAlchemy.      
